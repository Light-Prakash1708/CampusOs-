/**
 * Test stub for the `server-only` package.
 *
 * In the app this module makes it a build error to import server code into a
 * client bundle. Under vitest there is no client bundle, so it is a no-op.
 */
export {};
