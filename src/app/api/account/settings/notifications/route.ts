/**
 * The signed-in person's own notification settings, for every portal.
 * (`/api/student/settings/notifications` remains as the original path.)
 */
export { POST } from '../../../student/settings/notifications/route';
export const dynamic = 'force-dynamic';
